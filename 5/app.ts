let quote: string ="“A person who never made a mistake never tried anything new.”";
let famous_persong: string ="Albert Einstein";
let message= `${famous_persong} once again, ${quote}`;
console.log(message);
